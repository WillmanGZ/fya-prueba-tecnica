# HTTP server
PORT=8080

# PostgreSQL — in ECS these arrive via Secrets Manager, not plain env vars.
# Locally (docker-compose), plain values here are fine.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fya_prueba_tecnica
DB_USER=postgres
DB_PASSWORD=postgres
